import LogIn from "@/component/authentication/login/LogIn";

export const metadata = {
  title: "login",
  description: "Authentication user page ",
};

const Page = () => {
  return <LogIn />;
};

export default Page;
