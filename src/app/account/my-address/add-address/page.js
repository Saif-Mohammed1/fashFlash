import Addaddress from "@/component/address/add-address";

export const metadata = {
  title: "Add New Address",
  description: "Add New Address Page",
};
const Page = () => {
  return <Addaddress />;
};

export default Page;
